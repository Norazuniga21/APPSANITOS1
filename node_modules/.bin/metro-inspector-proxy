#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

"$basedir/../@highcharts/highcharts-react-native/node_modules/metro-inspector-proxy/src/cli.js"   "$@"
exit $?
